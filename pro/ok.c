#include <stdio.h>

int main() {
    printf("Эта программа не работает!\n");
    
    // Намеренные ошибки
    int x = 10 / 0;  // Деление на ноль
    
    char *ptr = NULL;
    *ptr = 'A';  // Разыменование нулевого указателя
    
    int arr[5];
    arr[10] = 100;  // Выход за границы массива
    
    return 0;
}